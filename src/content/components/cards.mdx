---
title: Cards
description: A collection of cards for displaying items in a grid layout.
tag: cs-cards
---
import ComponentPreview from '../../components/ComponentPreview.astro';
import PropsTable from '../../components/PropsTable.astro';

## Preview

<ComponentPreview label="Basic cards">
  <cs-cards id="demo-cards"></cs-cards>
</ComponentPreview>

<script>{`
  const el = document.getElementById('demo-cards');
  if (el) {
    el.cardDefinition = {
      header: item => item.name,
      sections: [
        { id: 'description', header: 'Description', content: item => item.description },
        { id: 'type', header: 'Type', content: item => item.type },
      ],
    };
    el.items = [
      { name: 'Instance A', description: 'Web server running in us-east-1', type: 't3.micro' },
      { name: 'Instance B', description: 'Database server in us-west-2', type: 't3.small' },
    ];
  }
`}</script>

<ComponentPreview label="With header and actions">
  <cs-cards id="demo-cards-header">
    <cs-header slot="header" variant="h2" counter="(3)">
      Instances
    </cs-header>
  </cs-cards>
</ComponentPreview>

<script>{`
  const hdr = document.getElementById('demo-cards-header');
  if (hdr) {
    hdr.cardDefinition = {
      header: item => item.name,
      sections: [
        { id: 'type', header: 'Type', content: item => item.type },
        { id: 'state', header: 'State', content: item => item.state },
        { id: 'az', header: 'Availability zone', content: item => item.az },
      ],
    };
    hdr.items = [
      { name: 'web-server-01', type: 't3.micro', state: 'Running', az: 'us-east-1a' },
      { name: 'api-gateway', type: 't3.small', state: 'Running', az: 'us-east-1b' },
      { name: 'batch-processor', type: 'c5.xlarge', state: 'Stopped', az: 'us-east-1a' },
    ];
    hdr.cardsPerRow = [{ cards: 1 }, { minWidth: 600, cards: 2 }, { minWidth: 900, cards: 3 }];
  }
`}</script>

<ComponentPreview label="Empty state">
  <cs-cards id="demo-cards-empty">
    <cs-header slot="header" variant="h2">Instances</cs-header>
    <cs-box slot="empty" text-align="center" color="inherit">
      <cs-space-between size="xxs" direction="vertical">
        <b>No instances</b>
        <cs-box color="text-body-secondary">No instances match the filter criteria.</cs-box>
      </cs-space-between>
    </cs-box>
  </cs-cards>
</ComponentPreview>

<script>{`
  const empty = document.getElementById('demo-cards-empty');
  if (empty) {
    empty.cardDefinition = {
      header: item => item.name,
      sections: [
        { id: 'type', header: 'Type', content: item => item.type },
        { id: 'state', header: 'State', content: item => item.state },
        { id: 'az', header: 'Availability zone', content: item => item.az },
      ],
    };
    empty.items = [];
  }
`}</script>

> **Note:** Card items and definition are set via JavaScript properties. See usage below.

## Usage

```html
<cs-cards></cs-cards>

<script>
  const cards = document.querySelector('cs-cards');
  cards.cardDefinition = {
    header: item => item.name,
    sections: [
      { id: 'description', header: 'Description', content: item => item.description },
      { id: 'type', header: 'Type', content: item => item.type },
    ],
  };
  cards.items = [
    { name: 'Card 1', description: 'First card description', type: 'Type A' },
    { name: 'Card 2', description: 'Second card description', type: 'Type B' },
    { name: 'Card 3', description: 'Third card description', type: 'Type A' },
  ];
</script>
```

## Properties

<PropsTable component="cards" />
