---
title: Date Range Picker
description: A form control that lets users select a date range or choose a relative time range.
tag: cs-date-range-picker
---
import ComponentPreview from '../../components/ComponentPreview.astro';
import PropsTable from '../../components/PropsTable.astro';

## Preview

<ComponentPreview label="Basic">
  <cs-date-range-picker id="demo-date-range" placeholder="Filter by date range"></cs-date-range-picker>
</ComponentPreview>

<script>{`
  const el = document.getElementById('demo-date-range');
  if (el) {
    el.relativeOptions = [
      { key: 'last-hour', amount: 1, unit: 'hour', type: 'relative' },
      { key: 'last-7', amount: 7, unit: 'day', type: 'relative' },
      { key: 'last-30', amount: 30, unit: 'day', type: 'relative' },
    ];
  }
`}</script>

<ComponentPreview label="With relative value">
  <cs-form-field label="Time range">
    <cs-date-range-picker id="demo-drp-relative" placeholder="Filter by date range"></cs-date-range-picker>
  </cs-form-field>
</ComponentPreview>

<script>{`
  const drp = document.getElementById('demo-drp-relative');
  if (drp) {
    drp.relativeOptions = [
      { key: 'previous-1-hour', amount: 1, unit: 'hour', type: 'relative' },
      { key: 'previous-6-hours', amount: 6, unit: 'hour', type: 'relative' },
      { key: 'previous-1-day', amount: 1, unit: 'day', type: 'relative' },
      { key: 'previous-7-days', amount: 7, unit: 'day', type: 'relative' },
      { key: 'previous-30-days', amount: 30, unit: 'day', type: 'relative' },
    ];
    drp.value = { type: 'relative', amount: 7, unit: 'day' };
  }
`}</script>

## Usage

The `value` and `relativeOptions` properties must be set via JavaScript:

```html
<cs-date-range-picker placeholder="Filter by date range"></cs-date-range-picker>

<script>
  const picker = document.querySelector('cs-date-range-picker');
  picker.value = {
    type: 'absolute',
    startDate: '2025-01-01',
    endDate: '2025-01-31',
  };
  picker.relativeOptions = [
    { key: 'last-7', amount: 7, unit: 'day', type: 'relative' },
    { key: 'last-30', amount: 30, unit: 'day', type: 'relative' },
  ];
</script>
```

## Properties

<PropsTable component="date-range-picker" />
