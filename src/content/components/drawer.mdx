---
title: Drawer
description: A panel with header and content, used inside app-layout for tools and split views.
tag: cs-drawer
---
import ComponentPreview from '../../components/ComponentPreview.astro';
import PropsTable from '../../components/PropsTable.astro';

## Preview

<ComponentPreview label="Basic">
  <cs-drawer header="Instance details">
    <cs-space-between size="l" direction="vertical">
      <cs-key-value-pairs id="drawer-kvp"></cs-key-value-pairs>
      <cs-expandable-section header-text="Network interfaces">
        <cs-box color="text-body-secondary">eni-0123456789abcdef0 — 10.0.1.42</cs-box>
      </cs-expandable-section>
    </cs-space-between>
  </cs-drawer>
</ComponentPreview>

<script>{`
  const kvp = document.getElementById('drawer-kvp');
  if (kvp) {
    kvp.items = [
      { label: 'Instance ID', value: 'i-0123456789abcdef0' },
      { label: 'Type', value: 't3.micro' },
      { label: 'State', value: 'Running' },
      { label: 'AZ', value: 'us-east-1a' },
    ];
  }
`}</script>

## Usage

```html
<cs-drawer header="Resource details">
  <cs-space-between size="l" direction="vertical">
    <cs-key-value-pairs columns="2">
      <!-- key-value content -->
    </cs-key-value-pairs>
  </cs-space-between>
</cs-drawer>
```

## Properties

<PropsTable component="drawer" />
