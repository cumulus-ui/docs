---
title: List
description: A component for displaying a list of items with optional icons and secondary content.
tag: cs-list
---
import ComponentPreview from '../../components/ComponentPreview.astro';
import PropsTable from '../../components/PropsTable.astro';

## Preview

<ComponentPreview label="Default">
  <cs-list id="list-default" aria-label="Files"></cs-list>
</ComponentPreview>

<ComponentPreview label="Compact">
  <cs-list id="list-compact" aria-label="Files" variant="compact"></cs-list>
</ComponentPreview>

<script>{`
  const items = ['report-2024.pdf', 'summary.docx', 'data.csv', 'notes.txt', 'readme.md'];
  const setup = (el) => {
    el.items = items;
    el.renderItem = (item) => ({ id: item, content: item, secondaryContent: 'Document' });
  };
  setup(document.getElementById('list-default'));
  setup(document.getElementById('list-compact'));
`}</script>

## Usage

```html
<cs-list id="my-list" aria-label="Files"></cs-list>
<script>
  const list = document.getElementById('my-list');
  list.items = ['file1.txt', 'file2.txt'];
  list.renderItem = (item) => ({ id: item, content: item });
</script>
```

## Properties

<PropsTable component="list" />
