---
title: Panel layout
description: A resizable split layout with a main content area and a side panel.
tag: cs-panel-layout
---
import ComponentPreview from '../../components/ComponentPreview.astro';
import PropsTable from '../../components/PropsTable.astro';

## Preview

<ComponentPreview label="Basic">
  <div style="height: 300px; border: 1px solid var(--color-border-divider-default); border-radius: 8px; overflow: hidden;">
    <cs-panel-layout panel-position="right" resizable default-panel-size="280">
      <div slot="main" style="padding: 16px;">
        <cs-header variant="h2">Instances</cs-header>
        <cs-box color="text-body-secondary" margin-top="s">Select an instance to view its details in the side panel.</cs-box>
      </div>
      <div slot="panel" style="padding: 16px;">
        <cs-header variant="h3">web-server-01</cs-header>
        <cs-key-value-pairs id="pl-kvp"></cs-key-value-pairs>
      </div>
    </cs-panel-layout>
  </div>
</ComponentPreview>

<script>{`
  const kvp = document.getElementById('pl-kvp');
  if (kvp) {
    kvp.items = [
      { label: 'Type', value: 't3.micro' },
      { label: 'State', value: 'Running' },
      { label: 'AZ', value: 'us-east-1a' },
    ];
  }
`}</script>

## Usage

```html
<cs-panel-layout panel-position="right" resizable default-panel-size="300">
  <div slot="main">Main content</div>
  <div slot="panel">Side panel</div>
</cs-panel-layout>
```

## Properties

<PropsTable component="panel-layout" />
