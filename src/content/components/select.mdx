---
component: select
description: A dropdown control that lets users choose a single option from a list.
---
import ComponentPreview from '../../components/ComponentPreview.astro';
import PropsTable from '../../components/PropsTable.astro';

## Preview

<ComponentPreview label="Basic select">
  <cs-select id="demo-select" placeholder="Choose an option"></cs-select>
</ComponentPreview>

> **Note:** Options are set via the `options` JavaScript property. See usage below.

<ComponentPreview label="Disabled option">
  <cs-select id="demo-select-disabled" placeholder="Choose an option"></cs-select>
</ComponentPreview>

<ComponentPreview label="Grouped options">
  <cs-select id="demo-select-grouped" placeholder="Choose a region"></cs-select>
</ComponentPreview>

<ComponentPreview label="With filtering">
  <cs-select id="demo-select-filter" placeholder="Search regions" filtering-type="auto"></cs-select>
</ComponentPreview>

<ComponentPreview label="With label context">
  <cs-select id="demo-select-labeled" placeholder="Select a region" selected-label="Region"></cs-select>
</ComponentPreview>

<script>{`
  document.getElementById('demo-select').options = [
    { label: 'US East (N. Virginia)', value: 'us-east-1' },
    { label: 'US West (Oregon)', value: 'us-west-2' },
    { label: 'Europe (Ireland)', value: 'eu-west-1' },
  ];
  document.getElementById('demo-select').selectedOption = { label: 'US East (N. Virginia)', value: 'us-east-1' };

  document.getElementById('demo-select-disabled').options = [
    { label: 'US East (N. Virginia)', value: 'us-east-1' },
    { label: 'US West (Oregon)', value: 'us-west-2' },
    { label: 'Asia Pacific (Tokyo)', value: 'ap-northeast-1', disabled: true },
    { label: 'Europe (Ireland)', value: 'eu-west-1' },
  ];
  document.getElementById('demo-select-disabled').selectedOption = { label: 'US East (N. Virginia)', value: 'us-east-1' };

  document.getElementById('demo-select-grouped').options = [
    { label: 'North America', options: [
      { label: 'US East (N. Virginia)', value: 'us-east-1' },
      { label: 'US West (Oregon)', value: 'us-west-2' },
      { label: 'Canada (Central)', value: 'ca-central-1' },
    ]},
    { label: 'Europe', options: [
      { label: 'Europe (Ireland)', value: 'eu-west-1' },
      { label: 'Europe (Frankfurt)', value: 'eu-central-1' },
    ]},
    { label: 'Asia Pacific', options: [
      { label: 'Asia Pacific (Tokyo)', value: 'ap-northeast-1' },
      { label: 'Asia Pacific (Singapore)', value: 'ap-southeast-1' },
    ]},
  ];
  document.getElementById('demo-select-grouped').selectedOption = { label: 'US East (N. Virginia)', value: 'us-east-1' };

  document.getElementById('demo-select-filter').options = [
    { label: 'US East (N. Virginia)', value: 'us-east-1' },
    { label: 'US West (Oregon)', value: 'us-west-2' },
    { label: 'Europe (Ireland)', value: 'eu-west-1' },
  ];
  document.getElementById('demo-select-filter').filteringPlaceholder = 'Find a region';

  document.getElementById('demo-select-labeled').options = [
    { label: 'US East (N. Virginia)', value: 'us-east-1' },
    { label: 'US West (Oregon)', value: 'us-west-2' },
    { label: 'Europe (Ireland)', value: 'eu-west-1' },
  ];
  document.getElementById('demo-select-labeled').selectedOption = { label: 'US West (Oregon)', value: 'us-west-2' };
`}</script>

## Usage

```html
<cs-select placeholder="Choose an option"></cs-select>

<script>
  const select = document.querySelector('cs-select');
  select.options = [
    { label: 'Option 1', value: '1' },
    { label: 'Option 2', value: '2' },
    { label: 'Option 3', value: '3', disabled: true },
  ];
  select.selectedOption = { label: 'Option 1', value: '1' };
</script>
```

## Properties

<PropsTable component="select" />
